---
import '../styles/global.css';
import { t, type Locale } from '../lib/ui-strings';
import LanguageToggle from '../components/LanguageToggle.astro';
interface Props { title: string; lang?: Locale }
const { title, lang = 'en' } = Astro.props;
const prefix = lang === 'de' ? '/de' : '';
---
<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
  </head>
  <body>
    <header class="site">
      <div class="container nav-shell">
        <a class="brand" href="/" aria-label="Dinner Table home"><span class="brand-mark">DT</span><span>Dinner Table</span></a>
        <nav aria-label="Primary navigation">
          <a href="/#dinners">{t(lang, 'dinners')}</a>
          <a href="/#recipes">{t(lang, 'recipes')}</a>
          <a href="/shopping-list">{t(lang, 'shopping')}</a>
          <a href={`${prefix}/cloyne`}>{t(lang, 'cloyne')}</a>
        </nav>
      </div>
    </header>
    <main class="container">
      <slot />
    </main>
    <footer class="container footer-row">
      <span>{t(lang, 'footer')}</span>
      <LanguageToggle />
    </footer>
  </body>
</html>
